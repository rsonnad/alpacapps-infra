{
  "name": "your-project-mobile",
  "version": "1.0.0",
  "description": "YOUR_APP_NAME mobile app - iOS & Android",
  "private": true,
  "scripts": {
    "sync": "node scripts/copy-web.js && npx cap sync",
    "sync:ios": "node scripts/copy-web.js && npx cap sync ios",
    "sync:android": "node scripts/copy-web.js && npx cap sync android",
    "open:ios": "npx cap open ios",
    "open:android": "npx cap open android",
    "build": "node scripts/copy-web.js"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.2",
    "@capacitor/cli": "^8.0.2",
    "@capacitor/core": "^8.0.2",
    "@capacitor/ios": "^8.0.2",
    "@capacitor/push-notifications": "^8.0.0",
    "@capacitor/splash-screen": "^8.0.0",
    "@capacitor/status-bar": "^8.0.0",
    "@capgo/capacitor-updater": "^8.43.1"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
